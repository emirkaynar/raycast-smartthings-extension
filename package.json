{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "smartthings-home-control",
  "title": "SmartThings Home Control",
  "description": "Control and monitor your Samsung SmartThings devices directly from Raycast.",
  "icon": "extension-icon.png",
  "license": "MIT",
  "commands": [
    {
      "name": "list-devices",
      "title": "List Devices",
      "description": "Authenticate with SmartThings and control your smart home devices.",
      "mode": "view",
      "subtitle": "List and control SmartThings devices",
      "preferences": [
        {
          "name": "brokerBaseUrl",
          "title": "Auth Broker Base URL",
          "description": "Base URL of the Cloudflare Worker auth broker.",
          "type": "textfield",
          "required": true,
          "default": "https://smartthings-auth-worker.emirkaynar.workers.dev"
        }
      ]
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.103.0",
    "@raycast/utils": "^2.2.1"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.0.4",
    "@types/node": "22.13.10",
    "@types/react": "19.0.10",
    "eslint": "^9.22.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2"
  },
  "scripts": {
    "dev": "ray develop",
    "lint": "ray lint",
    "fix-lint": "ray lint --fix",
    "build": "ray build",
    "publish": "npx @raycast/api@latest publish",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1"
  },
  "author": "theema-dev",
  "platforms": [
    "Windows",
    "macOS"
  ],
  "categories": [
    "Other"
  ]
}